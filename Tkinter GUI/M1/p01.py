import tkinter as tk  # Import tkinter module

# Create the main window
root = tk.Tk() #creates a root window.
root.title("My First GUI") #sets the window title.
root.geometry("300x200")  # sets the size. Width x Height

# Start the GUI loop
root.mainloop() #keeps the window open and responsive.

